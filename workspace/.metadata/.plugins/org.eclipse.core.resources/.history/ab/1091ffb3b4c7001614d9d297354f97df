package org.ntua.tant.rest.resources;

import java.util.List;
import javax.ws.rs.Consumes;
import javax.ws.rs.DELETE;
import javax.ws.rs.GET;
import javax.ws.rs.POST;
import javax.ws.rs.PUT;
import javax.ws.rs.Path;
import javax.ws.rs.PathParam;
import javax.ws.rs.Produces;
import javax.ws.rs.QueryParam;
import javax.ws.rs.core.MediaType;

import org.ntua.tant.rest.DBService.*;
import org.ntua.tant.rest.model.*;

@Path("/messanges")
@Consumes(MediaType.APPLICATION_XML)
@Produces(MediaType.APPLICATION_XML)
public class MessageResource {
	
	@GET
	public List<Message> getMessages(@QueryParam("year") int year,
			                         @QueryParam("code") int code,
			                         @QueryParam("start") int start,
			                         @QueryParam("size") int size){
		
		DB db = new DB();
		
		
		if(year > 0) {
			//return messageService.getAllMessagesForYear(year);
		}
		if(start >= 0 && size > 0) {
			//return messageService.getAllMessagesPaginated(start, size);
		}
		if(code > 0){
			//return messageService.getAllMessagesForCode(code);
		}
		return db.selectAllMessages(db.connect());
	}
	
	@POST
	public Message addMessage(Message message){
		return null;//messageService.addMessage(message);
	}
	
	@PUT
	@Path("/{messageId}")
	public Message updateMessage(@PathParam("messageId") long id, Message message){
		message.setId(id);
		return null;//messageService.updateMessage(message);
	}
	
	@DELETE
	@Path("/{messageId}")
	public Message deleteMessage(@PathParam("messageId") long id){
		return null;//messageService.removeMesage(id);
	}
	
	@GET
	@Path("/{messageId}")
	public Message getMessage(@PathParam("messageId") long Id) {
		return null;//messageService.getMessage(Id);
	}

}
